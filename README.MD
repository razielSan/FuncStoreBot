> Проект может быть использован как шаблон для создания Telegram-ботов.

# Func Store Bot

**Модульный Телеграм-Бот, построенный на базе aiogram 3.x, который позволяет**

- искать изображения по названию
- получать информацию по ip
- получать сведения о пользователе по Api Id Telegram
- генерировать простые и сложные пароли
- получать списки прокси-адресов 
- узнавать прогноз погоды
- узнавать уровень загрязнения воздуха в городе
- получать карту погоды
- получать ссылки на видео для youtube по названию


## 🎯 Для кого этот проект

- Для разработчиков Telegram-ботов
- Для изучения модульной архитектуры
- Для практики сервисного подхода
- Для примера тестируемого async-кода


## Общее положение

- Модульная архитектура
- Модули с возможностью быстрой загрузки
- Встроенная обработка ошибок
- Асинхронное проектирование
- Широкий охват тестированием (модульное / интеграционное / внешнее)


## 🚀 Запуск проекта

### 1. Убедитесь, что установлен Python **3.8+**

Проверка;

<pre>python --version</pre>


### 2. Клонирования репозитория

<pre>git clone https://github.com/razielSan/FuncStoreBot.git
cd FuncStoreBot</pre>

### 3. Создание виртуального окружения

<pre>python -m venv venv<br>
source venv/bin/activate      # Linux/MacOS<br>
venv\Scripts\activate         # Windows<br></pre>


### 4. Установка зависимостей 

Из корневой директории

<pre>pip install -e .[dev]</pre>

### 5. Настройка файла .env

---

**ТОКЕН для бота**

Создайте файл .env в папке src/app/bot/

.env

<pre>TOKEN=YOUR_TOKEN</pre>

---

**Для поиска имени по изображения**

Создайте файл .env в папке src/app/bot/modules/find_image/childes/find_name_image/

.env

<pre>GOOGLE_CX="YOUR_CX"<br>
GOOGLE_API_KEY="YOUR_API_KEY"</pre>

СX И API_KEY для Google<br>
[Способ получения](https://developers.google.com/custom-search/v1/introduction?hl=ru)

---

**Для поиска постеров с сайта https://www.kinopoisk.ru/**

Создайте файл .env в папке src/app/bot/modules/find_image/childes/kinopoisk/

.env

<pre>API_KEY=YOUR_API_KEY</pre>

[Получить тут](https://kinopoiskapiunofficial.tech/)

---

**Для получения информации по ip**

Создайте файл .env в папке src/app/bot/modules/ip/childes/info/

.env

<pre>ACCESS_KEY=YOUR_ACCES_KEY</pre>

[Получить тут](https://ipapi.com/)

---

**Для получения прокси-адресов с модуля webshare**

Создайте файл .env в src/app/bot/modules/proxies/childes/webshare/

.env

<pre>ApiKey=YOUR_API_KEY</pre>

[Получить тут](https://proxy.webshare.io/)

---

**Для получения уровня загрязнения воздуха**

Создайте файл .env в src/app/bot/modules/weather_forecast/childes/aqi

.env

<pre>APPID=YOUR_APPID</pre>

[Получить тут](http://api.openweathermap.org/)

---

**Для получения прогноза погоды**

Создайте файл .env в src/app/bot/modules/weather_forecast/childes/weather/

.env

<pre>APPID=YOUR_APPID</pre>

[Получить тут](http://api.openweathermap.org/)

---

**Для получения карты погоды**

Создайте файл .env в src/app/bot/modules/weather_forecast/childes/weather_map/

.env

<pre>APPID=YOUR_APPID</pre>

[Получить тут](http://api.openweathermap.org/)

---

**Для получения ссылок на видео для youtube**

Создайте файл .env в src/app/bot/modules/youtube/childes/search

.env

<pre>API_KEY=YOUR_API_KEY</pre>

[Как получить](https://b-id.ru/bid.youtube/how_get_key.php)

---

### 6. Запуска бота

Из корневой директории

</pre>bot</pre>


## 🤖 Функциональность


### 1. Создание новых модулей

Из корня проекта

<pre>cli add-module test/childes/test test_module</pre>


### 2. Удаление модуля

Из корня проекта

<pre>cli remove-module test/childes/test</pre>

## Особенности создания и использования модулей

- При создание модуля, **дочерние модули разделяеются именем "childes", другие имена**
  **не допускаются**
  Имя `childes` используется системой загрузки модулей и является зарезервированным.

  <pre>cli add-module test/childes/test</pre>

- Создание нового роутера должно осуществлятся в папке handlers.**Имя переменной должно**
  **быть 'router', другие имена не допускаются**
  Имя `router` используется системой загрузки модулей и является зарезервированным.

  <pre>router = Router(name=__name__)</pre>
  
- Если у модуля есть дочерние модули (`childes`), система автоматически формирует 
  inline-клавиатуру для перехода к ним. Кнопки создаются на основе `settings` 
  дочерних модулей.

- По умолчанию, предпологается что дочерний модуль можно подключить к инлайн клавиатуре
  родительского модуля через settings.MENU_CALLBACK_DATA, находящийся в файле settings.py
  дочернего модуля.Подключайте как вам удобнее

  <pre>@router.callback_query(F.data == settings.MENU_CALLBACK_DATA)
  asyn def childe_test_module():
    ...</pre>

- Родительские модули автоматически прикрепляются к main модулю при их создании.
  По умолчанию, предпологается что родительский модуль можно подключить к main модулю
  через settings.MENU_REPLY_TEXT, находящийся в файле settings.py родительского модуля
  Подключайте как вам удобнее
  
  <pre>@router.message(F.text == settings.MENU_REPLY_TEXT)
  asyn def parent_test_module():
    ...</pre>

- Файлы logging.py, response.py, router.py изменять не рекомендуется. При их изменении
  стабильность приложения не гарантируется. Вносите изменения на свой страх и риск

- В классе ModuleSettings оставляйте нетронутыми следующие поля

    class ModuleSettings(BaseModel):
        SERVICE_NAME: str = "proxies"
        NAME_FOR_TEMP_FOLDER: str = "proxies"
        ROOT_PACKAGE: str = "app.bot.modules.proxies"

  Остальные измения и расширения класса на работу систему не повлияют.

-  Таким образом, родительские модули работают через **reply-кнопки**,  
   а дочерние — через **inline-кнопки**.

- При запукске приложения автоматически создаются log файлы(app/logs) и 
  temp папки(app/bot/temp) для модулей

- Лог файлы создаются только для родительских модулей а temp папки для каждого
  модуля


## Полезные атрибуты доступные в router

**Передаюся через Dispatcher автоматически**

### 1. session - асинхронная сессия для запроса

<pre>@router.message(F.text == "test)
async def test(message, session):
        ....</pre>

### 2. get_main_keyboards - главная reply клавиатура модуля с кнопками родительских модулей

<pre>@router.message(F.text == "test")
async def test(message, get_main_keyboards)
    await message.answer(text="Главное меню", reply_markup=get_main_keyboards)</pre>


## 🧪 Тестирование

Проект покрыт:
- unit-тестами
- integration-тестами
- external-тестами

Запуск:

<pre>pytest</pre>

## 🤖 Архитектура проекта

Проект построен по сервисному принципу:
- handlers → services → api
- минимальные зависимости между слоями
- внешние сервисы легко подменяются в тестах
  
## :telephone: **Обратная связь**

Если нашли ошибку - создайте Issue или напишите мне в Telegram

[Мой телеграм](https://t.me/razielasus)
